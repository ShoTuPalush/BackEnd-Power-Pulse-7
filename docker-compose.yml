version: '3.4'

services:
  backendpowerpulse7:
    image: backendpowerpulse7
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
